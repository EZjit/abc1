# Test task
## Simple module to parse text files and exctact unique phone numbers
### Usage:
1) `git clone https://github.com/EZjit/abc1.git'
2) `cd <path_to_cloned_repo>`
3) `python3 -m abc1.py <path_to_file>`
